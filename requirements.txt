# Core RL stack (Python 3.10+, including 3.12)
gymnasium>=0.29
stable-baselines3>=2.3.0
numpy
pandas
matplotlib
pygame
tensorboard

# PyTorch is NOT pinned here because the right wheel depends on the GPU.
# Install it FIRST, before this file, with the matching CUDA build.
#
# RTX 5090 (Blackwell, sm_120) — needs CUDA 12.8 build, NOT cu124.
# The cu124 nightly index returns older snapshots that lack sm_120 kernels.
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# (if cu128 stable is unavailable, fall back to its nightly:)
#   pip install --pre torch --index-url https://download.pytorch.org/whl/nightly/cu128
#
# Older NVIDIA GPUs (CUDA 12.1):
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#
# CPU only:
#   pip install torch
#
# NOTE: legacy v7.py and rl_*.py scripts use the old `gym` package and will not
# run in this venv. Use a separate environment (Python 3.10 + gym==0.21.0) if
# you need to reproduce paper-era results.
