# Tested with Python 3.9 (laptop) — desktop 5090 already satisfies these
# (torch 2.11.0+cu128 per logs_revision config.json).
# torch: install the CUDA build matching your driver, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
stable-baselines3>=2.3
gymnasium>=0.29
numpy
scipy
tensorboard
# optional, only needed for render_mode="human"
pygame
